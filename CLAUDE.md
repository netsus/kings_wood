# CLAUDE.md

이 저장소에서 작업할 때는 `AGENTS.md`를 가장 먼저 기준으로 삼는다.

## Read First

1. `AGENTS.md`
2. `README.md`
3. `Site_Plan.png`
4. `Phase2_Floor_Plan.png`

## Core Rules

- 목표는 `킹스우드 부지 추천용 모바일 특화 웹사이트`다.
- 기본 언어는 한국어다.
- `충청북도 옥천군 군북면 증약리 1483`와 제공된 평면도 이미지를 중심 소스로 사용한다.
- 사주와 땅의 기운 해석은 참고용 해석으로 다루고, 사실 정보와 섞어 단정하지 않는다.
- 부모님의 생년월일과 출생시간이 없으면 개인 맞춤 해석을 임의 생성하지 않는다.

## Build Direction

- 정적 배포 친화적인 구현을 우선한다.
- 특별한 이유가 없으면 프론트엔드만으로 완성 가능한 구조를 택한다.
- GitHub Pages 배포를 기본 경로로 고려한다.

## Quality Bar

- 모바일 화면에서 보기 좋고 조작하기 쉬워야 한다.
- 실제 브라우저에서 확인하기 전에는 완료로 보지 않는다.
- 시각 검토가 필요하면 `pnpm review:ui`로 Playwright 아티팩트를 먼저 만든다.
- 시각적 변경이 크면 스크린샷으로 검증한다.
- 지도형 이미지, 부지 카드, 추천 결과가 각각 독립 컴포넌트로 유지되게 만든다.

## Working Style

- 작은 단위로 구현하고 바로 실행해서 확인한다.
- 추정이 필요한 내용은 표시하고, 필요한 입력값은 빠르게 드러낸다.
- 배포 방식이나 데이터 구조에 큰 영향이 있으면 먼저 공유한다.
